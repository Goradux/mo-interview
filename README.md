Open two terminals:

1. in the frontend folder, run: `npm intall` and `npm run dev`
2. in the backend folder, run: `npm install` and `npm run build:run`

To test it, send some message in the front end. It should return the generated message word by word.
